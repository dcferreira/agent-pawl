workflow: rule15guardsaccepted
start: a
guards:
  - id: never-rewrite-changelog
    match: "(sed|awk) .*CHANGELOG.md"
    only_in: []
  - id: push-only-in-commit
    match: "git push"
    only_in: [a]
steps:
  - id: a
    kind: deterministic
    run: echo hi
    next: done
terminal: {done: {status: ok}}
