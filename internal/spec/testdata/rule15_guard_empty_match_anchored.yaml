workflow: rule15guardemptymatchanchored
start: a
guards:
  - id: blank-only
    match: "^$"
    only_in: []
steps:
  - id: a
    kind: deterministic
    run: echo hi
    next: done
terminal: {done: {status: ok}}
