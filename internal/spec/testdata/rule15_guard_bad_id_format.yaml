workflow: rule15guardbadidformat
start: a
guards:
  - id: "../escaped"
    match: "git push"
    only_in: []
steps:
  - id: a
    kind: deterministic
    run: echo hi
    next: done
terminal: {done: {status: ok}}
