workflow: retry-backoff-invalid
start: a
steps:
  - id: a
    kind: deterministic
    run: echo hi
    retry: {max_attempts: 3, backoff: "not-a-duration"}
    next: done
terminal: {done: {status: ok}}
